# =============================================================================
# CONFIGURAÇÃO DE AMBIENTE - Locais Noturnos
# =============================================================================
# Instruções: Copie este arquivo para .env e preencha com suas credenciais reais
# Atenção: Nunca faça commit do arquivo .env (já incluído no .gitignore)

# -----------------------------------------------------------------------------
# GOOGLE MAPS API
# -----------------------------------------------------------------------------
# Chave para Google Places API (Nearby Search, Text Search, Details, Photos)
# Console: https://console.cloud.google.com/google/maps-apis
# Necessário habilitar: Places API, Maps SDK (iOS/Android), Maps JavaScript API
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=YOUR_API_KEY_HERE

# -----------------------------------------------------------------------------
# BANCO DE DADOS (Backend Node.js)
# -----------------------------------------------------------------------------
# Configuração MySQL para conexão do backend Express
DB_HOST=localhost
DB_PORT=3306
DB_NAME=locais_noturnos
DB_USER=root
DB_PASSWORD=your_password_here

# -----------------------------------------------------------------------------
# AUTENTICAÇÃO
# -----------------------------------------------------------------------------
# Secret para assinatura de tokens JWT (mínimo 32 caracteres em produção)
# Gere um secret seguro: openssl rand -base64 32
JWT_SECRET=change_this_in_production_minimum_32_chars

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
# Nível de log do backend (ERROR | WARN | INFO | DEBUG)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# CONECTIVIDADE MÓVEL
# -----------------------------------------------------------------------------
# URL base da API para dispositivos físicos (substitua pelo IP da sua máquina)
# Localhost não funciona em dispositivos iOS/Android físicos - use IP local
# Exemplo para mesma rede Wi-Fi: http://192.168.0.5:3000/api
# Deixe vazio para auto-detecção via Expo manifest (recomendado para dev)
EXPO_PUBLIC_API_BASE_URL=

