# Exemplo de configuração do .env
# Copie este arquivo para .env e preencha com suas chaves reais

# Chave da API do Google Maps para Places API
# Obtenha em: https://console.cloud.google.com/google/maps-apis
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=YOUR_API_KEY_HERE

# MySQL Database Configuration (para backend)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=locais_noturnos
DB_USER=root
DB_PASSWORD=your_password_here

# Optional: Logging Level (ERROR, WARN, INFO, DEBUG)
LOG_LEVEL=INFO
